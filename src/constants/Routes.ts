export const enum Routes {
  HOME = "/",

  PRODUCTS = "/products",
  ABOUT = "/about",
  SIGN_UP = "/sign_up",
  SIGN_IN = "/sign_in",
  PROFILE_PAGE = "/user",
  CART = "/cart",
  ADMIN = "/admin",
}

export const serverError = 500;
